class HomeAccordionItem {
     constructor(name, pageComponent, previewComponent) {
          this.name = name;
          this.pageComponent = pageComponent;
          this.previewComponent = previewComponent;
          this.visible = true;
     }
}

export default HomeAccordionItem;